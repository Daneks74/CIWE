{
  "name": "ciwe",
  "version": "0.1.0",
  "private": true,
  "description": "CIWE — Mobile mockup for The Theodore 4000 smart thaw device",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}",
    "dev": "serve -s . -l 3000",
    "build": "echo 'Static site — no build step required.'"
  },
  "dependencies": {
    "serve": "14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
